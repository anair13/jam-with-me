<%= form_for(@user_entry) do |f| %>
  <% if @user_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_entry.errors.count, "error") %> prohibited this user_entry from being saved:</h2>

      <ul>
      <% @user_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br />
    <%= f.number_field :song_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
